(function () {

  'use strict';

  var db = openDatabase('onm2016', '1.0', 'description', 1 * 1024 * 1024);

  angular.module('service', ['toastr', 'ngCordova'])

    //.constant('db', {
    //  'db' : tmp
    //})
  ;

})();
